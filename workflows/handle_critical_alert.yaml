version: 1.0
description: Workflow untuk menangani alert kritis.
tasks:
  notify:
    action: monitor_test.notify_discord
    input:
      webhook_url: "{{ config.webhook_url }}"
      message: "ALERT! {{ trigger.reason }} detected on {{ trigger.host }}."
