name: "critical_alert"
pack: "monitor_test"
description: "Trigger untuk alert kritis."
payload_schema:
  type: "object"
  properties:
    resource:
      type: "string"
    usage:
      type: "number"
